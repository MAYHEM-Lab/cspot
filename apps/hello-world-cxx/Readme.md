# cspot hello world with Modern C++

To build:

You'll need cmake > 3:
```
yum install -y cmake3
```

Actually building:
```
mkdir build
cd build
cmake3 ..
make -j2
```

Binaries will be in `build/cspot` directory